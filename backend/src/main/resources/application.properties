spring.application.name=backend
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}
server.port=8080
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.jpa.open-in-view=false

# Cache configuration
spring.cache.type=caffeine
spring.cache.cache-names=vocabulary,vocabulary-level
spring.cache.caffeine.spec=expireAfterWrite=10m,maximumSize=10000

# Database Mode (mysql or mongodb)
app.database.type=${APP_DATABASE_TYPE:mongodb}
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:${MONGO_DB_URL:mongodb+srv://myjsdb:qwertyuiopqaz@myjsdb.dhfyvpg.mongodb.net/japanesedb?retryWrites=true&w=majority&appName=MyJsDB}}
spring.data.mongodb.auto-index-creation=true
flyway.enabled=${FLYWAY_ENABLED:false}

# Sentry Configuration
sentry.dsn=${SENTRY_DSN:}
